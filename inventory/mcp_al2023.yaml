---
# Amazon Linux 2023 test inventory.
# Used to test: fresh-host verify, offline install, Non-RHEL OS, and upgrade path.
all:
  children:
    mcp:
      hosts:
        ip-10-222-1-136.ec2.internal:
          ansible_host: 54.210.164.222
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/pet-east1.pem
      vars:
        mcp_version: latest
        mcp_python_packages:
          - python3.11
          - python3.11-pip
        mcp_transport: sse

        mcp_platform_host: 10.222.0.26
        mcp_platform_user: admin
        mcp_platform_password: admin
